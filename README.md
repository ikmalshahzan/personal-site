# Personal Site

React + Vite conversion of the existing portfolio landing page.

## Run locally

1. `npm install`
2. `npm run dev`

## Build for GitHub Pages

1. `npm run build`
2. Deploy the generated `docs/` folder as GitHub Pages from the repository settings.
